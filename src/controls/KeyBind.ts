/** Define a key for control binding. */
export interface KeyBind {
  /** Key press event key code to bind. */
  keyCode: number,
  /** Toggle if the key is positive or negative. */
  inverted: boolean,
}
